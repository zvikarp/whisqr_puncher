name: whisqr_puncher
description: The Whisqr puncher application.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.2
  dio: ^3.0.10
  auto_route: ^1.0.2
  flutter_text_helpers: ^2.0.5
  flutter_secure_storage: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator: ^1.0.2

flutter:
  uses-material-design: true
